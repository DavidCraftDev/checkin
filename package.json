{
  "name": "checkin",
  "version": "2.0.0",
  "private": true,
  "workspaces": [
    "apps/backend",
    "apps/frontend"
  ],
  "scripts": {
    "dev": "npm run dev -w @checkin/backend",
    "dev:backend": "npm run dev -w @checkin/backend",
    "dev:frontend": "npm run dev -w @checkin/frontend",
    "build": "npx prisma generate && npm run build -w @checkin/frontend && npm run build -w @checkin/backend",
    "start": "npm run start -w @checkin/backend",
    "lint": "echo \"No root lint command configured for monorepo migration\"",
    "update": "npx prisma migrate deploy && npx prisma db seed && npm run build"
  },
  "dependencies": {
    "@prisma/client": "7.7.0",
    "dotenv": "17.4.0",
    "prisma": "7.7.0",
    "tsx": "4.21.0"
  },
  "devDependencies": {
    "typescript": "6.0.2"
  }
}
